{
  "private": true,
  "name": "sass-lint-config-firecloud",
  "description": "The sass-lint config used within Tobii Cloud Services.",
  "version": "0.0.4",
  "license": "Unlicense",
  "dependencies": {},
  "devDependencies": {
    "eclint": "2.3.0"
  },
  "peerDependencies": {
    "sass-lint": "1.10.1"
  },
  "scripts": {
    "test": "eclint check $(git ls-files | grep -v -e \"^package-lock.json$\" -e \"^LICENSE$\" -e \"^UNLICENSE$\")"
  }
}
